// api.js — Gemini integratie verwijderd.
// Alle functies zijn lege stubs zodat bestaande imports niet breken.

export async function generateGeminiText(_apiKey, _prompt) {
  return { success: false, error: "AI niet beschikbaar" };
}

export async function generateSpeech(_apiKey, _text) {
  return { success: false, error: "AI niet beschikbaar" };
}
